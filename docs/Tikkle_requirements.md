# 티끌(Tikkle) 프로젝트 기능 명세서 (화면 별 기능)

## 0. 온보딩 화면 (최초 진입)
사용자가 앱에 처음 진입하여 서비스 이용을 위한 기초 설정을 마치는 구간입니다.

| 기능 ID | 기능명 | 설명 | 사전 조건 | 정상 흐름 (Happy Path) | 예외 및 에러 처리 / 데이터 힌트 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **FEAT-ONB-001** | 온보딩 및 성향 설정 | 회원가입 후 투자 성향(설문) 및 잔돈 규칙(단위/비율)을 설정합니다. | 앱 최초 설치 및 회원가입 완료 | 1. 5문항 설문 진행 후 성향(안정/중립/공격) 할당<br>2. 잔돈 규칙(예: 1000원 미만) 및 매매 방식(자동/수동) 선택<br>3. DB 저장 및 Redis 캐싱 | **[예외]** 중도 이탈 시 다음 접속 때 해당 스텝부터 재개.<br>**[Hint]** User_Profile JSON: `{ "risk_type": "AGGRESSIVE", "rule": "UNDER_1000", "is_auto": true }` |
| **FEAT-ONB-002** | 모의투자 API 키 연동 | 사용자가 KIS에서 발급받은 App/Secret Key를 앱에 등록하여 개별 투자 환경을 구축합니다. | 온보딩 성향 설정 완료 | 1. KIS 모의투자 계좌번호, App Key, Secret Key 입력<br>2. 백엔드에서 KIS 토큰 발급 테스트 (유효성 검증)<br>3. 유효성 확인 시 키를 DB에 암호화 저장 | **[예외]** 토큰 발급 실패 시 사용자에게 가이드라인과 함께 재입력 요청.<br>**[Hint]** secret_key는 반드시 AES-256 등으로 암호화 저장. |

## 1. 홈 화면 (투자 요약 대시보드)
사용자의 전체 자산 현황과 투자 포트폴리오를 한눈에 보여주는 메인 화면입니다.

| 기능 ID | 기능명 | 설명 | 사전 조건 | 정상 흐름 (Happy Path) | 예외 및 에러 처리 / 데이터 힌트 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **FEAT-HOME-001** | 총 자산 및 수익률 요약 | 총 매매금액, 총 평가금액, 실시간 수익률을 노출합니다. | 1회 이상 투자(매수) 완료 | 1. DB를 통해 투자에 반영된 총 결제(잔돈) 원금 조회<br>2. 사용자별 KIS API로 보유 주식 실시간 평가액 조회<br>3. 데이터 결합 후 총자산 및 수익률 렌더링 | **[예외]** KIS API 장애/지연 시 DB에 캐싱된 마지막 스냅샷 노출 후 '새로고침' 아이콘 활성화. |
| **FEAT-HOME-002** | 포트폴리오 요약 | 보유 중인 종목별 투자 비중을 파이 차트 등으로 시각화합니다. | 보유 종목 1개 이상 존재 | 1. KIS API에서 보유 종목 리스트 및 매입가/평가액 호출<br>2. 종목별 비중(%) 산출 후 파이 차트 렌더링 | **[예외]** 아직 투자가 발생하지 않은 경우 빈 화면(Empty State) 디자인 노출. |

## 2. 결제 관련 정보 화면 (결제 및 잔돈 통계)
사용자의 상세 소비 내역과 잔돈의 투자 반영 여부를 꼼꼼하게 트래킹하는 화면입니다.

| 기능 ID | 기능명 | 설명 | 사전 조건 | 정상 흐름 (Happy Path) | 예외 및 에러 처리 / 데이터 힌트 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **FEAT-PAY-001** | 결제 및 잔돈 통계 리스트 | 결제 내역과 발생한 잔돈의 투자 상태(반영/미반영) 및 기간별 통계를 제공합니다. | 1회 이상 결제 푸시 수신 | 1. DB에서 조건별 데이터 조회 (월별/카테고리별 등)<br>2. 화면 상단에 총 결제금액, 총 투자 반영/미반영 잔돈 노출<br>3. 하단에 상세 결제 리스트 렌더링 | **[Hint]** 결제 요약 Table 필수 컬럼: total_payment, monthly_payment, total_invested_change, monthly_invested, total_uninvested, monthly_uninvested. |
| **FEAT-PAY-002** | 수동 매매 종목 선택 UI | 수동 설정 시 24시간 내에 잔돈을 투자할 종목을 고르는 UI입니다. | PENDING 상태 트랜잭션 존재 | 1. AI가 추천한 3개 종목 카드 노출<br>2. 1개 선택 시 매수 트랜잭션 진행 (INVESTED)<br>3. '투자 취소' 선택 시 미투자 잔돈으로 전환 (NOT_INVESTED) | **[예외]** 24시간이 경과하여 진입한 경우 "투자기한이 만료되어 미투자 잔돈으로 보관되었습니다." 얼럿 노출 후 해당 건 NOT_INVESTED 처리. |

## 3. 주식 관련 정보 화면 (인사이트)
초보 투자자를 위한 교육 및 시황 콘텐츠를 제공하여 투자의 두려움을 낮추고 시장 흐름을 안내하는 화면입니다.

| 기능 ID | 기능명 | 설명 | 사전 조건 | 정상 흐름 (Happy Path) | 예외 및 에러 처리 / 데이터 힌트 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **FEAT-INS-001** | 투데이 마켓 토픽 (뉴스 수집) | 크롤링 및 API를 통해 수집된 최신 금융 뉴스와 경제 토픽을 리스트업합니다. | 백그라운드 정보 수집 자동화 동작 완료 | 1. 프론트엔드 진입 시 뉴스 API 호출<br>2. 백엔드가 DB/Redis에 캐싱된 최신 기사 썸네일, 제목, 요약 제공<br>3. 클릭 시 인앱 브라우저(WebView)로 기사 원문 노출 | **[예외]** 외부 API 장애나 크롤링 실패로 데이터가 없을 경우 Fallback 가이드 UI 노출.<br>**[Hint]** 속도 및 안정성을 위해 네이버 검색 API(뉴스) 또는 경제 매체 RSS 피드를 우선 활용. |
| **FEAT-INS-002** | 초보자 맞춤형 투자 기초 코어 | 사용자 투자 성향에 맞춘 기초 금융 용어(ETF란? 채권이란?) 및 가이드라인 아티클을 추천합니다. | 온보딩 성향 설정 완료 | 1. 사용자 성향(안정/중립/공격) 조회<br>2. 성향에 매핑된 정적 교육 콘텐츠 필터링<br>3. 카드 형태의 UI로 렌더링 | **[Hint]** 기초 교육 콘텐츠는 변동성이 적으므로 관리자(운영진)가 사전에 DB에 직접 초기 세팅(Seed Data) 해두는 방식을 권장. |

## 4. 설정 화면
앱 동작 규칙과 사용자 계정을 관리하는 화면입니다.

| 기능 ID | 기능명 | 설명 | 사전 조건 | 정상 흐름 (Happy Path) | 예외 및 에러 처리 / 데이터 힌트 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **FEAT-SET-001** | 투자 설정 관리 | 자동/수동 매매, 잔돈 단위 규칙을 변경합니다. | 온보딩 완료 | 1. 설정 변경 시 DB 반영 및 Redis 즉각 동기화<br>2. 이후 수신되는 푸시 결제 건부터 새로운 룰 적용 | **[Hint]** 잦은 IO를 방지하기 위해 사용자의 현재 설정값은 Redis에 올려두고 조회하는 것을 권장. |
| **FEAT-SET-002** | 회원 탈퇴 및 계좌 관리 | 서비스 탈퇴 시 데이터 파기합니다. | 온보딩 완료 | 1. 탈퇴 요청 시 사용자의 DB 연동 정보 및 계정 삭제처리(논리삭제) |

## 5. 공통 / 백그라운드 시스템 (UI 없음)
화면에는 보이지 않지만, 데이터의 흐름과 자동화된 트랜잭션을 책임지는 코어 백엔드/프론트엔드 백그라운드 기능입니다.

| 기능 ID | 기능명 | 설명 | 사전 조건 | 정상 흐름 (Happy Path) | 예외 및 에러 처리 / 데이터 힌트 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **FEAT-SYS-001** | 푸시 스크래핑 및 HMAC 보안 계층 | 스마트폰 알림을 파싱하여 백엔드로 전송하고, 서버는 서명을 통해 위변조 검증 및 중복 처리를 방어합니다. | 안드로이드 알림 권한 허용 및 앱 백그라운드 실행 |1. 프론트엔드가 정규식을 가동해 가맹점명, 금액, 카드사, 카드 4자리, 알림 발생시간 추출 <br> 2. 데이터를 조합해 결정적 해시 고유 ID(transactionId) 생성 <br> 3. 파싱된 본문과 Timestamp로 HMAC SHA-256 서명 생성 <br> 4. 헤더에 서명을 담아 POST /api/payments 전송 <br> 5. 백엔드 인터셉터에서 서명 무결성 검증 | **[예외]** 기기 전원 꺼짐/절전 모드로 인한 유실은 단기 프로젝트 스펙상 복구하지 않음.[보안 예외] 타임스탬프 5분 초과 시 Replay Attack으로 간주하여 401 Unauthorized 차단.<br>[Hint] `Body: { "userId": 1, "cardCompany": "KB_CARD", "cardNumberLast4": "1234", "merchant": "스타벅스", "amount": 4300, "transactionId": "cf23df2207d99..." }` |
| **FEAT-SYS-002** | 잔돈 산출 및 Fail-Fast 예외 처리 | 중복 결제와 타사 카드는 원천 차단하되, 타겟 카드의 잔돈 0원 결제 건은 소비 관리 목적으로 '투자안함' 상태로 적재합니다. | FEAT-SYS-001 검증 통과 | 1. [1단계] transactionId 중복 검사 ➔ 차단 후 조기 종료 <br> 2. [2단계] 유저의 지정 카드 정보와 대조 ➔ 불일치 시 차단 후 조기 종료 <br> 3. 유저 규칙 기반 잔돈(spare_change) 계산 <br> 4. [3단계] 잔돈이 0원이면 PaymentEvent 엔티티의 상태를 NOT_INVESTED(투자안함)로 변경 후 DB에 save하고 로직 종료 <br> 5. 잔돈이 존재하면 상태를 PENDING으로 세팅 후 다음 캐시 파이프라인 진행 | **[Hint]** 잔돈이 0원이더라도 DB에 소비 데이터가 기록되므로, 누락 없는 완벽한 가계부 및 지출 통계를 제공할 수 있음. <br> **[Status Enum 4종]** CLASSIFYING, PENDING, INVESTED, NOT_INVESTED |
| **FEAT-SYS-003** | 2-Tier 비동기 가맹점 카테고리 분류 | 로컬 캐시와 메시지 큐(AI)를 혼합하여 제로 레이턴시(Zero-Latency) 분류를 수행합니다. | FEAT-SYS-002 잔액 검증 통과 | 1. [1차 방어선] Caffeine Cache를 통해 가맹점명 Prefix 매칭 검색 (HIT 시 즉시 분류 후 PENDING 저장)<br>2. [2차 방어선] MISS 시 상태를 CLASSIFYING으로 저장 후 RabbitMQ 비동기 이벤트 발행<br>3. 백그라운드 Worker가 AI API 호출로 카테고리 확정 후 DB(상태를 PENDING으로 전환)/Cache 업데이트 | **[예외]** AI API 서버 장애 시 3회 Timeout 재시도 후 서킷 브레이커를 열어 '기타(미분류)'로 강제 할당 및 PENDING 전환 (장애 전파 방지). |
| **FEAT-SYS-004** | 수동 매매 대기 및 만료 스케줄링 | 수동 매매 사용자의 경우, AI가 추천한 종목의 유저 승인을 대기합니다. | 사용자 설정 is_auto: false | 1. AI가 온보딩 성향+카테고리 반영하여 3개 추천 종목 선정<br>2. 트랜잭션 테이블에 PENDING 및 expired_at(현재+24시간) 저장<br>3. Spring 스케줄러가 매일 동작하며 24시간 미응답 건을 NOT_INVESTED로 일괄 변경 | **[예외]** [예외] 유저가 취소한 건도 즉시 NOT_INVESTED 처리되어 미투자 통계에 합산됨. |
| **FEAT-SYS-005** | Spring Batch 기반 일괄 매수 실행 | 장 시작 시점에 대량의 주문을 KIS API를 통해 병렬 및 Rate Limit을 준수하며 체결합니다. | 매일 09:00 (장 시작) / 종목 확정 건 | 1. JpaPagingItemReader가 전날 쌓인 매수 대상(PENDING 또는 승인 건) 페이징 조회<br>2. KIS API 키 복호화 및 유효성 검사<br>3. Resilience4j RateLimiter를 적용하여 KIS 소수점 매수 API 호출<br>4. 성공 시 INVESTED 상태 변경 및 자산 누적 업데이트 | **[예외]** KIS API의 엄격한 초당 호출 제한(TPS)을 넘지 않도록 Jitter(지연 난수) 및 RateLimiter 필수 적용. 동시 실행 방지를 위한 Redis 분산 락 적용. |
| **FEAT-SYS-006** | 금융 데이터 자동 수집 배치 | 매일 지정된 시간에 금융 뉴스 및 시황 정보를 가져와 내부 DB를 최신화합니다. | Spring Boot 스케줄러 동작 | 1. 하루 N회(예: 오전 8시, 오후 6시) 외부 API 및 타겟 사이트 연동<br>2. 필요한 기사 제목/썸네일/링크 파싱<br>3. DB에 일괄 저장(Batch Insert) 후 오래된 데이터 파기 | **[예외]** 크롤링 대상 사이트 DOM 변경 시 로그 전송 후 기존 데이터 유지.<br>**[Hint]** 빈번한 호출에 의한 IP 차단 방지를 위해 Delay 설정 및 외부 OpenAPI 중심 수집 권장. |