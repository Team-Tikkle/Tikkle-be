# Tikkle 업비트 자동 매수 흐름 및 프론트엔드 연동 가이드

본 문서는 Tikkle 서비스에서 사용자가 '매수 승인'을 누른 시점부터 케이뱅크 출금, 업비트 입금, 최종 시장가 매수 체결까지의 모든 비동기 흐름(SSE 통신)을 프론트엔드 관점에서 완벽하게 다룹니다.

---

## 1. 전체 연동 워크플로우

1. **승인 요청 (`POST /approve`)**: 프론트엔드가 백엔드에 사용자의 매수 승인 의사를 전달합니다.
2. **SSE 스트림 연결 (`GET /stream`)**: 승인 요청이 `200 OK`로 떨어지는 **즉시** SSE 연결을 맺어 실시간 응답 대기 모드로 진입합니다.
3. **입금 대기 (2차 인증)**: 백엔드는 케이뱅크 출금을 위한 카카오톡/네이버 2차 인증 알림을 사용자에게 발송하고, 3초마다 인증 완료 여부를 폴링(Polling)합니다.
4. **시장가 매수 (5초 퀵 체크)**: 인증이 완료되어 원화가 업비트로 들어오면, 즉각 코인 시장가 매수 주문을 넣고 최대 5초간 체결 결과를 기다립니다.
5. **결과 전송 및 커넥션 종료**: 체결 성공(`SUCCESS`), 지연(`PENDING_TRADE`), 입금 실패(`DEPOSIT_FAILED`), 매수 실패(`TRADE_FAILED`) 등 결과를 프론트로 전송하고 백엔드에서 **자동으로 커넥션을 끊어(Complete)** 리소스를 해제합니다.

---

## 2. API 호출 순서 및 상세

### Step 1. 매수 승인 (입금 요청 트리거)
- **API**: `POST /api/payments/{eventId}/approve`
- **역할**: 업비트에 원화 입금 요청을 찔러서 사용자의 스마트폰으로 2차 인증 푸시가 날아가게 합니다.
- **예외 처리**: 
  - `401 Unauthorized` (응답 바디 JSON의 code가 `UPBIT_INVALID_KEY`인 경우): 업비트 인증 키가 만료되거나 권한이 취소된 상태입니다. 프론트엔드는 사용자에게 **"업비트 인증이 만료되었습니다. 다시 연동해주세요."** 라는 모달창/팝업을 띄우고, 업비트 계정 재연동 화면으로 이동시켜야 합니다.
  - 기타 400/500 에러(예: 연동된 계좌 없음 등)가 난다면, 즉시 에러 팝업을 띄우고 SSE 연결은 하지 마세요.

### Step 2. SSE 커넥션 맺기
- **API**: `GET /api/payments/{eventId}/stream` (Headers: `Accept: text/event-stream`, `Authorization: Bearer 토큰`)
- **역할**: 비동기로 진행되는 카카오톡 인증 결과와 매수 체결 결과를 실시간으로 받아옵니다.

> [!WARNING]
> **서버의 연결 종료(Complete)**
> 이벤트가 최종 결과(SUCCESS, DEPOSIT_FAILED, TRADE_FAILED, PENDING_TRADE, TIMEOUT)에 도달하면 백엔드가 스스로 소켓을 닫습니다. 이때 프론트엔드의 `EventSource` 객체는 기본적으로 3초 뒤 다시 재연결(Reconnect)하려는 성질이 있습니다. **따라서 최종 이벤트를 받은 직후에는 반드시 프론트 쪽 코드에서도 명시적으로 `eventSource.close()`를 호출하여 재접속을 차단해 주어야 합니다.**

---

## 3. SSE 이벤트 스펙 및 UI/UX 대응 가이드

프론트엔드는 SSE 스트림을 통해 다음과 같은 `event` 식별자와 `data`(JSON)를 수신하게 됩니다.

### 🔵 CONNECTED (최초 1회)
- **상황**: 서버와 성공적으로 SSE가 연결되었을 때 발송됩니다. (브라우저 타임아웃 방지용)
- **UI 처리**: 로딩 스피너(Lottie 애니메이션 등)를 화면에 띄우고 "사용자의 승인을 기다리고 있습니다..." 같은 문구를 노출합니다.
```json
// data
"SSE 연결 성공"
```

### 🟡 PROCESSING (3초 간격)
- **상황**: 백엔드가 업비트에 입금 승인 여부를 계속 묻고 있는 중입니다.
- **UI 처리**: "카카오톡/네이버 앱에서 2차 인증을 완료해 주세요"라는 문구를 깜빡이거나 강조하여 사용자 액션을 유도합니다.
```json
// data
"업비트 2차 인증을 대기 중입니다."
```

### 🟢 SUCCESS (매수 퀵 체결 성공)
- **상황**: 가장 흔한 해피 경로. 입금이 확인된 직후 5초 이내에 시장가 매수가 완벽하게 체결된 경우입니다.
- **UI 처리**: 축하/성공 애니메이션과 함께 결제 영수증 모달을 띄워줍니다.
```json
// data
{
  "status": "SUCCESS",
  "targetCoinName": "비트코인",
  "investedVolume": 0.00005409,
  "investedPrice": 94220000.0,
  "message": "비트코인 0.00005409개 매수 성공했습니다."
}
```

### 🟠 PENDING_TRADE (매수 지연 - 백그라운드 추적 전환)
- **상황**: 코인을 주문했으나 거래소 호가창 물량 부족 등으로 5초 내에 안 사졌을 때 발생합니다.
- **UI 처리**: (매우 중요) 사용자를 더 이상 로딩 창에 붙잡아 두지 않습니다! 다음과 같은 팝업을 띄우고 유저가 자유롭게 앱을 쓸 수 있게 놓아주세요.
- **권장 안내 문구**: `"주문이 정상 접수되었으나, 거래소 사정으로 체결이 지연되고 있습니다. 체결이 완료되면 결제 내역에서 확인하실 수 있습니다!"`
```json
// data
{
  "status": "PENDING_TRADE",
  "message": "주문이 정상적으로 접수되었습니다. 거래소 사정에 따라 체결이 지연되고 있으며, 체결이 완료되면 결제 내역에서 확인하실 수 있습니다."
}
```

### 🔴 DEPOSIT_FAILED (입금 실패)
- **상황**: 업비트 입금 거절(케이뱅크 잔액 부족 등)로 원화 자체가 넘어가지 못한 상태입니다.
- **UI 처리**: 에러 내용을 그대로 띄워주면 됩니다.
- **권장 안내 문구**: `"업비트 입금이 거절되거나 취소되었습니다. 연동 계좌의 잔액을 확인해 주세요."`
```json
// data
{
  "status": "DEPOSIT_FAILED",
  "message": "업비트 입금이 거절되거나 취소되었습니다."
}
```

### 🔴 TRADE_FAILED (매수 실패)
- **상황**: 최소 주문 금액 미달 등 매수 주문 에러가 발생한 경우입니다. 원화(KRW)는 이미 케이뱅크에서 업비트로 넘어가서 보관된 상태이므로, 이 사실을 분명히 고지해야 합니다.
- **UI 처리**: (매우 중요) 사용자의 돈이 업비트에 묶여있음을 꼭 안내해야 합니다.
- **권장 안내 문구**: `"매수 주문이 거절되었습니다. 고객님의 케이뱅크 계좌에서 출금된 원화는 현재 업비트 계좌에 안전하게 보관되어 있습니다. 업비트 앱에서 직접 매수하시거나 원화를 출금해 주세요."`
```json
// data
{
  "status": "TRADE_FAILED",
  "message": "업비트 매수 주문이 거절되거나 취소되었습니다."
}
```

### 🔴 TRADE_FAILED (키 인증 만료 - UPBIT_INVALID_KEY)
- **상황**: SSE 연결 및 2차 인증 대기, 매수 진행 중에 업비트 API 키 권한이 만료되거나 박탈된 것을 감지한 상태입니다.
- **UI 처리**: (매우 중요) 에러 팝업 모달을 띄우고, **업비트 재연동 설정 화면**으로 라우팅을 수행합니다.
```json
// data
{
  "status": "UPBIT_INVALID_KEY",
  "message": "업비트 인증 키가 만료되거나 권한이 없습니다. 앱에서 업비트 계정을 다시 연동해주세요."
}
```

### ⚫ TIMEOUT (인증 시간 초과)
- **상황**: 사용자가 3분(180초) 동안 2차 인증을 하지 않았을 때 발생합니다.
- **UI 처리**: "2차 인증 시간이 초과되었습니다. 다시 시도해 주세요." 모달 표시
```json
// data
{
  "status": "TIMEOUT",
  "message": "업비트 2차 인증 시간이 초과되었습니다."
}
```

---

## 4. 백그라운드 지연 매수 (PENDING_TRADE) 작동 원리
프론트엔드 개발자가 알아두면 좋은 백엔드 로직입니다.

- `PENDING_TRADE` 이벤트를 받고 프론트엔드가 통신을 종료한 후에도, **백엔드의 스케줄러는 죽지 않고 10초마다 10분 동안 업비트를 찌르며 체결 상태를 추적**합니다.
- **10분 이내 체결 성공 시**: 백엔드에서 결제 내역 상태를 업데이트합니다. 앱에서 결제 내역을 새로고침하여 확인할 수 있습니다.
- **10분 초과 시**: 강제 주문 취소(시장 변동성에 물리는 것 방지) 처리를 진행합니다. (케이뱅크에서 출금된 원화는 환불되지 않고 사용자 업비트 계좌에 그대로 보관됩니다.)

---

## 5. 앱 진입 시(부팅) 사전 점검 가이드 (선택/권장)
사용자가 앱을 켤 때마다 백그라운드 스케줄러가 권한을 무효화한 사실을 프론트가 캐치할 수 있습니다.
- **API**: `GET /api/users/me`
- **로직**: 응답 프로퍼티 중 `hasUpbitKey: false`를 수신했는데 기존에 업비트를 연동했던 사용자인 경우 (혹은 설정 탭 진입 시), 즉시 팝업을 띄워 사용자에게 재연동을 안내할 수 있습니다.
